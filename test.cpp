#include<vector>
#include<iostream>
using namespace std;

int main(int argc, char const *argv[])
{
    vector<int> test;
    test.pop_back();
    test.pop_back();
    test.pop_back();
    test.pop_back();
    test.pop_back();
    test.pop_back();
    return 0;
}
