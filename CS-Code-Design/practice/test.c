#include <stdio.h>
#include <stdlib.h>
#include <string.h>

void reverse(char * string){
    
}

int main(int argc, char * argv[]){
    int length = strlen(argv[1]);
    for(int i = 0; i < length; i++)
        printf("%c",argv[1][i]);
    printf("\n");
}